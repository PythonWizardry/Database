If you look at code, it differs a litle bit from branch lab-4-5-vscode.
But i also create changes you can't see, the main issue is i created another yamal file with  passwords and add it in.gitignore, so if you need to start this app, change parametrs in app.yml
